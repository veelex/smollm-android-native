name: Build llama.cpp Android native lib

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up NDK
        uses: nttld/setup-ndk@v1
        id: setup-ndk
        with:
          ndk-version: r26d

      - name: Configure CMake
        run: |
          cmake -S jni -B build-android \
            -DCMAKE_TOOLCHAIN_FILE=${{ steps.setup-ndk.outputs.ndk-path }}/build/cmake/android.toolchain.cmake \
            -DANDROID_ABI=arm64-v8a \
            -DANDROID_PLATFORM=android-26 \
            -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build-android --config Release -j4

      - name: Collect .so files
        run: |
          mkdir -p output/arm64-v8a
          find build-android -name "*.so" -exec cp {} output/arm64-v8a/ \;

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: jniLibs-arm64-v8a
          path: output/arm64-v8a/
